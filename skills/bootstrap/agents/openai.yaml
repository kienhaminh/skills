interface:
  display_name: "Bootstrap"
  short_description: "Build simple, reusable, agent-readable codebases"
  default_prompt: "Use $bootstrap to map, reuse before building, define conventions for, clean, or restructure this repository from live source evidence."
