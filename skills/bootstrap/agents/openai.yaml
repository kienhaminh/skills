interface:
  display_name: "Bootstrap"
  short_description: "Bootstrap repositories from live evidence"
  default_prompt: "Use $bootstrap to map and rehabilitate this repository with truthful commands, reusable boundaries, enforceable conventions, and checkout-derived documentation."
