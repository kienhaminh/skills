Methods: Contract Testing -> DRY -> YAGNI.

Implement node B exactly as declared in graph.json. Read only its trust-order and scope paths. Use the `implement` skill, preserve non-goals, and change only its exclusive write scope. Run focused checks, but do not claim verification, edit graph/runtime/memory directly, commit, push, or reconstruct parent chat. Return the standard result envelope; propose any learned state only through `memory_delta`.
