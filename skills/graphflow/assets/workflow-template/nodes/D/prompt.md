Methods: YAGNI -> KISS -> DRY.

Implement node D exactly as declared in graph.json. Load only `implement`, named inputs, and the bounded memory capsule. Stay inside the exclusive web scope and preserve the approved prototype. Do not edit graph/runtime/memory directly, widen scope, commit, push, or self-verify. Return only the standard result envelope.
