Methods: TDD -> Boundary Value Analysis -> YAGNI.

Implement node C exactly as declared in graph.json. Load only `tdd`, `implement`, named inputs, and the bounded memory capsule. Stay inside the exclusive server scope and acceptance contract. Do not edit graph/runtime/memory directly, widen scope, commit, push, or self-verify. Return only the standard result envelope.
