interface:
  display_name: "Graphflow"
  short_description: "Run persistent verified coding-work graphs"
  default_prompt: "Use $graphflow to create, edit, or run a persistent provider-neutral coding workflow graph."
policy:
  allow_implicit_invocation: false
