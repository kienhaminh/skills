interface:
  display_name: "Graphflow"
  short_description: "Run persistent verified Codex workflow graphs"
  default_prompt: "Use $graphflow to create, edit, or run a persistent Codex workflow graph."
policy:
  allow_implicit_invocation: false
