interface:
  display_name: "Sync Docs"
  short_description: "Reconcile durable docs with implemented behavior"
  default_prompt: "Use $sync-docs to reconcile the repository's durable product or domain documentation with current implementation evidence."
