# Repository contract

- Integration tests run only through `npm test`.
- The external harness supplies `RESERVATION_TEST_DATABASE_URL`; never invent or substitute it.
- Stop before production implementation and do not commit.
