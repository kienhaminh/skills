interface:
  display_name: "Test Driven Development"
  short_description: "Turn a user story into an enumerated test list and real red tests first"
  default_prompt: "Use $tdd to turn this user story into an enumerated test list and failing red tests, no production code."
