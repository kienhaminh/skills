interface:
  display_name: "Test Driven Development"
  short_description: "Turn agreed behavior into meaningful red tests"
  default_prompt: "Use $tdd to enumerate this behavior contract and write meaningful failing tests while leaving production behavior for implementation."
