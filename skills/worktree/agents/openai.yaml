interface:
  display_name: "Worktree"
  short_description: "Coordinate isolated Git worktrees safely"
  default_prompt: "Use $worktree to inventory, isolate, run, and clean concurrent Git worktrees with explicit ownership and resource allocation."
