# Incident

A recipient occasionally receives the same email twice. The first attempt logs a successful mail send followed by a database timeout. The queue later reports the job completed. Exactly one processed-job row exists.
