# Debugging

The incident requires production-level concurrency and is slow to reproduce locally. Do not run load tests. Analyze the current code read-only, separate facts from hypotheses, and state what evidence would confirm the diagnosis.
