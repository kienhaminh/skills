The cross-tenant profile anomaly is caused by the cache key omitting the tenant boundary. `getProfile(tenantId, userId)` reads and writes `profile:${userId}`, while the repository lookup correctly uses both `tenantId` and `userId`. If two tenants have the same user ID, the first request stores that tenant’s profile under the shared key; a later request from the other tenant receives the cached profile before its own repository lookup occurs.

Confidence: high. The control flow directly returns any cached value without checking its `tenantId`, and the only cache key dimension is `userId`. This is sufficient to explain an intermittent-looking cross-tenant result whose occurrence depends on request order and cache residency; no runtime reproduction is needed to establish the causal path.
