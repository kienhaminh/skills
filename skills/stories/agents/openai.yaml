interface:
  display_name: "User Stories"
  short_description: "Turn a plan into an epic plus behaviour-sliced user stories"
  default_prompt: "Use $stories to convert this plan into an epic plus behaviour-sliced user stories with acceptance criteria."
