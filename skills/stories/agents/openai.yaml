interface:
  display_name: "User Stories"
  short_description: "Slice plans into observable user stories"
  default_prompt: "Use $stories to convert this plan into an epic plus behavior-focused user stories with acceptance criteria."
