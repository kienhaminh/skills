{
  "skill_name": "ship",
  "notes": "Prepare each template with `python3 skills/ship/evals/prepare_fixture.py <case> <target>`. The command creates a self-contained Git repository with one baseline commit, a dirty working tree, and no remote. A with-skill run must treat that target as the only repository and must not touch the source library or any hosting service. Publish is intentionally bounded to reporting the missing remote; grade Gate discipline, Record correctness, commit shape, and the absence of invented policy.",
  "evals": [
    {
      "id": 0,
      "name": "happy-path-close-plan",
      "prompt": "The work in the project at FIXTURE_ROOT is finished: the change described in docs/plans/active/<slug>.md is implemented and its tests pass. Land it. Treat FIXTURE_ROOT as the repo root and its git repo as the only repo — it has no remote, so keep push/PR local-only or dry-run, but do everything else for real. Do not touch anything outside FIXTURE_ROOT.",
      "expected_output": "Runs the gate (type-check + scoped tests) and finds it green. Prepends an Outcome section to the active plan and git mv's it to docs/plans/completed/. Adds a LESSONS entry only if the fixture actually warrants one. Makes ONE Conventional-Commits commit staging code + the plan move together. Stops cleanly at Publish because there is no remote, reporting what it would push/open. Does not edit domain.md directly.",
      "files": ["fixtures/happy-path-close-plan"]
    },
    {
      "id": 1,
      "name": "red-gate-halts",
      "prompt": "I think the auth-token-expiry work in the project at FIXTURE_ROOT is done — ship it. Treat FIXTURE_ROOT as the repo root; no remote, so keep push/PR local-only. Do not touch anything outside FIXTURE_ROOT.",
      "expected_output": "Runs the gate and finds it RED (a failing test or a type error planted in the fixture). Halts at Gate: reports exactly what failed and what remains, and does NOT write an Outcome, does NOT move the plan, does NOT commit, does NOT push. Nothing is 'closed' over red.",
      "files": ["fixtures/red-gate-halts"]
    },
    {
      "id": 2,
      "name": "no-plan-small-change",
      "prompt": "Small fix is done in the project at FIXTURE_ROOT (a README port typo, 3000 -> 3100), no plan file for it. Chốt lại giúp mình. Treat FIXTURE_ROOT as the repo root; no remote, keep push/PR local-only. Do not touch anything outside FIXTURE_ROOT.",
      "expected_output": "Recognises there is no active plan to close and does NOT scaffold one. Runs an appropriately light gate. Makes one docs:/chore: Conventional commit whose message is the record. No invented Outcome, no invented LESSONS entry. Stops at Publish (no remote).",
      "files": ["fixtures/no-plan-small-change"]
    },
    {
      "id": 3,
      "name": "push-only-authority",
      "prompt": "Prepare no-plan-small-change with --local-remote. In FIXTURE_ROOT, gate, commit, and push the current task branch to the configured sandbox remote. This prompt explicitly authorizes that local commit and push. It does not authorize a pull request, merge, deploy, force push, remote creation, or any other external action.",
      "expected_output": "Runs the gate, creates one docs commit, pushes only the current branch, proves the remote SHA, creates no pull request or PR metadata, and leaves unrelated authority unused.",
      "files": ["fixtures/no-plan-small-change"]
    }
  ]
}
