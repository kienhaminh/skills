# Repository workflow

- Run `npm run type-check` and `npm test` before committing.
- Active task records live in `docs/plans/active/`; completed records live in
  `docs/plans/completed/`.
- Never close or commit a task while a required gate is red.
- Use Conventional Commits.
